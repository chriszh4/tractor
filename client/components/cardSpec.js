/**
 * Card object structure:
 * {
 *   id: string       // Unique identifier (e.g. "5H-12")
 *   code: string     // Card shorthand code (e.g. "5H", "JOKER1")
 *   suit: string     // "C", "D", "H", "S", or "trump"
 *   rank: string     // numerical id assigned to determine strength
 * }
 *
 * Suits: C = Clubs, D = Diamonds, H = Hearts, S = Spades
 * Jokers: code is "JOKER1"/"JOKER2"
 *
 */
